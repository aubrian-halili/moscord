{
  "name": "moscord-ecomm",
  "description": "Moscord Exercises",
  "repository": {},
  "license": "UNLICENSED",
  "version": "0.0.1",
  "main": "server.js",
  "dependencies": {},
  "scripts": {
    "start": "cross-env NODE_PATH=src/ node -r dotenv/config src",
    "debug": "cross-env NODE_PATH=src/ nodemon --inspect -r dotenv/config src"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.20.1",
    "nodemon": "^2.0.2"
  }
}
